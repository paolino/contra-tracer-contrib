name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Test with CHaP's contra-tracer (IOHK fork)
  # The Nix devshell uses IOG cache which provides CHaP's version
  build-iohk:
    runs-on: ubuntu-latest
    name: build (iohk/CHaP)

    steps:
      - uses: actions/checkout@v4

      - uses: paolino/dev-assets/setup-nix@v0.0.1
        with:
          cachix-auth-token: "${{ secrets.CACHIX_AUTH_TOKEN }}"

      - name: Build
        run: nix develop --quiet -c cabal build -f iohk

      - name: Test
        run: nix develop --quiet -c cabal test -f iohk

  # Test with Hackage's contra-tracer (arrow-based)
  # Uses GHCup instead of Nix to get vanilla Hackage packages
  build-hackage:
    runs-on: ubuntu-latest
    name: build (hackage)

    steps:
      - uses: actions/checkout@v4

      - uses: haskell-actions/setup@v2
        with:
          ghc-version: '9.8.4'
          cabal-version: '3.12'

      - name: Build
        run: cabal build

      - name: Test
        run: cabal test

  lint:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: paolino/dev-assets/setup-nix@v0.0.1
        with:
          cachix-auth-token: "${{ secrets.CACHIX_AUTH_TOKEN }}"

      - name: Check formatting
        run: nix develop --quiet -c fourmolu -m check src test

      - name: Lint
        run: nix develop --quiet -c hlint src test
